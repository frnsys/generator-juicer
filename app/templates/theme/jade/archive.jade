include inc/head

    | <?php if (have_posts()) : ?>
    | <?php while (have_posts() ) : the_post(); ?>
    include inc/post
    | <?php endwhile; ?>
    | <?php else : ?>
    h1 No posts found.
    | <?php endif; ?>

    include inc/foot
